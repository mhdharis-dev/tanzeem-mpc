{
  "version": 2,
  "buildCommand": "if [ ! -d _flutter ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1 _flutter; fi && ./_flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
